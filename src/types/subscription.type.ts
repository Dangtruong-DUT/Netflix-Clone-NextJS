export type SubscriptionPlanType = {
    id: string
    name: string
    resolution: string
    price: number
    currency: string
    quality: string
    supportedDevices: string[]
    simultaneousScreens: number
    downloads: number
}
